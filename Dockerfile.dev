FROM node:22-alpine

WORKDIR /app

# Install dependencies — source is bind-mounted at runtime, not copied here
COPY .npmrc package.json package-lock.json ./
RUN npm ci

EXPOSE 3000
CMD ["npm", "run", "dev"]
